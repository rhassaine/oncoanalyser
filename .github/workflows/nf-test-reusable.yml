name: Run nf-test on selected test set
on:
  workflow_call:
    inputs:
      name:
        description: "Name of test set"
        type: string
        required: true
      tags:
        description: "Tags to pass as argument for nf-test --tag parameter"
        type: string
        required: true
      runson_instance_type:
        description: "RunsOn runner instance type"
        type: string
        required: true
      runson_volume_size:
        description: "RunsOn runner instance volume"
        type: string
        required: true

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  NFT_VER: "0.9.3"
  NFT_WORKDIR: "~"
  NXF_ANSI_LOG: false
  NXF_SINGULARITY_CACHEDIR: ${{ github.workspace }}/.singularity
  NXF_SINGULARITY_LIBRARYDIR: ${{ github.workspace }}/.singularity

jobs:
  nf-test-changes:
    name: nf-test-changes (${{ inputs.name }})
    runs-on: # use self-hosted runners
      - runs-on=${{ github.run_id }}-nf-test-changes
      - runner=1cpu-linux-x64
      - extras=s3-cache
    outputs:
      shard: ${{ steps.set-shards.outputs.shard }}
      total_shards: ${{ steps.set-shards.outputs.total_shards }}
    steps:
      - uses: runs-on/action@v2

      - name: Clean Workspace # Purge the workspace in case it's running on a self-hosted runner
        run: |
          ls -la ./
          rm -rf ./* || true
          rm -rf ./.??* || true
          ls -la ./
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          fetch-depth: 0

      - name: get number of shards
        id: set-shards
        uses: ./.github/actions/get-shards
        env:
          NFT_VER: ${{ env.NFT_VER }}
        with:
          max_shards: 7
          tags: ${{ inputs.tags }}

      - name: debug
        run: |
          echo ${{ steps.set-shards.outputs.shard }}
          echo ${{ steps.set-shards.outputs.total_shards }}

  download-reference:
    name: Download HMF reference data
    needs: [nf-test-changes]
    if: ${{ needs.nf-test-changes.outputs.total_shards != '0' }}
    runs-on:
      - runs-on=${{ github.run_id }}-download-reference
      - runner=8cpu-linux-x64
      - volume=300gb
      - extras=s3-cache
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          fetch-depth: 0

      - uses: runs-on/action@v2

      - name: Restore HMF reference cache
        id: cache-hmf-ref
        uses: actions/cache@v4
        with:
          path: reference_data/
          key: hmf-reference-grch38-v25.1-wgs-bwamem2-star-${{ hashFiles('nextflow.config', 'conf/hmf_data.config', 'conf/hmf_genomes.config') }}
          restore-keys: |
            hmf-reference-grch38-v25.1-wgs-bwamem2-star-

      - name: Setup Nextflow for reference download
        if: steps.cache-hmf-ref.outputs.cache-hit != 'true'
        uses: nf-core/setup-nextflow@v2
        with:
          version: "25.04.0"
          # version: "24.10.5"

      - name: Download HMF reference data from R2
        if: steps.cache-hmf-ref.outputs.cache-hit != 'true'
        run: |
          nextflow run . \
            -profile docker \
            --mode prepare_reference \
            --ref_data_types wgs,bwamem2_index,star_index \
            --genome GRCh38_hmf \
            --outdir ./ \
            -c <(echo "aws.client.anonymous=true")

      - name: Upload Nextflow logfile for debugging purposes
        if: ${{ always() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: nextflow_logfile_${{ inputs.name }}.txt
          path: .nextflow.log*
          include-hidden-files: true

      - name: Verify reference data
        run: |
          echo "Reference data size:"
          du -sh reference_data/ || echo "reference_data/ not found"
          echo "Reference data structure:"
          find reference_data/ -type d -maxdepth 3 || echo "No directories found"
          echo "Detailed file listing:"
          ls -lah reference_data/2.3.0dev/ || echo "Cannot list reference_data/2.3.0dev/"
          echo "BWA-MEM2 index contents:"
          ls -lah reference_data/2.3.0dev/bwa-mem2_index-2.2.1/ | head -10 || echo "BWA-MEM2 index directory not found"
          echo "STAR index contents:"
          ls -lah reference_data/2.3.0dev/star_index-gencode_38-2.7.3a/ | head -10 || echo "STAR index directory not found"
          echo "GRIDSS index contents:"
          ls -lah reference_data/2.3.0dev/gridss_index-2.13.2/ | head -10 || echo "GRIDSS index directory not found"

  nf-test:
    name: "${{ inputs.name }} tests | ${{ matrix.profile }} | ${{ matrix.NXF_VER }} | ${{ matrix.shard }}/${{ needs.nf-test-changes.outputs.total_shards }}"
    needs: [nf-test-changes, download-reference]
    if: ${{ needs.nf-test-changes.outputs.total_shards != '0' }}
    runs-on: # use self-hosted runners
      - runs-on=${{ github.run_id }}-nf-test
      - runner=${{ inputs.runson_instance_type }}
      - volume=${{ inputs.runson_volume_size }}
      - extras=s3-cache
    strategy:
      fail-fast: false
      matrix:
        shard: ${{ fromJson(needs.nf-test-changes.outputs.shard) }}
        profile: [conda, docker, singularity]
        isMain:
          - ${{ github.base_ref == 'master' || github.base_ref == 'main' }}
        # Exclude conda and singularity on dev
        exclude:
          - isMain: false
            profile: "conda"
          - isMain: false
            profile: "singularity"
        NXF_VER:
          - "25.04.0"
          - "latest-everything"
    env:
      NXF_ANSI_LOG: false
      TOTAL_SHARDS: ${{ needs.nf-test-changes.outputs.total_shards }}

    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          fetch-depth: 0

      - uses: runs-on/action@v2

      - name: Restore HMF reference cache
        uses: actions/cache@v4
        with:
          path: reference_data/
          key: hmf-reference-grch38-v25.1-wgs-bwamem2-star-${{ hashFiles('nextflow.config', 'conf/hmf_data.config', 'conf/hmf_genomes.config') }}
          restore-keys: |
            hmf-reference-grch38-v25.1-wgs-bwamem2-star-

      - name: Run nf-test
        id: run_nf_test
        uses: ./.github/actions/nf-test
        continue-on-error: ${{ matrix.NXF_VER == 'latest-everything' }}
        env:
          NFT_WORKDIR: ${{ env.NFT_WORKDIR }}
          NXF_VERSION: ${{ matrix.NXF_VER }}
        with:
          profile: ${{ matrix.profile }}
          shard: ${{ matrix.shard }}
          total_shards: ${{ env.TOTAL_SHARDS }}
          tags: ${{ inputs.tags }}

      - name: Report test status
        if: ${{ always() }}
        run: |
          if [[ "${{ steps.run_nf_test.outcome }}" == "failure" ]]; then
            echo "::error::Test with ${{ matrix.NXF_VER }} failed"
            # Add to workflow summary
            echo "## ❌ Test failed: ${{ matrix.profile }} | ${{ matrix.NXF_VER }} | Shard ${{ matrix.shard }}/${{ env.TOTAL_SHARDS }}" >> $GITHUB_STEP_SUMMARY
            if [[ "${{ matrix.NXF_VER }}" == "latest-everything" ]]; then
              echo "::warning::Test with latest-everything failed but will not cause workflow failure. Please check if the error is expected or if it needs fixing."
            fi
            if [[ "${{ matrix.NXF_VER }}" != "latest-everything" ]]; then
              exit 1
            fi
          fi
